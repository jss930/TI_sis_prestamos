import { useState } from 'react'
export default function Catalogo({items}){const [categoria,setCategoria]=useState('Todos');const visibles=categoria==='Todos'?items:items.filter(i=>i.categoria===categoria);return <><div className="filtros">{['Todos','Libro','Equipo','Deporte'].map(c=><button className={categoria===c?'activo':''} onClick={()=>setCategoria(c)} key={c}>{c}</button>)}</div><section className="catalogo-grid">{visibles.map(item=><article className="item" key={item.id}><div className={`item-imagen ${item.categoria.toLowerCase()}`}>{item.categoria==='Libro'?'▤':item.categoria==='Equipo'?'▣':'●'}</div><small>{item.categoria} · {item.codigo}</small><h3>{item.nombre}</h3><div className="disponibilidad"><span className={item.disponibles?'punto':'punto no'}/>{item.disponibles?`${item.disponibles} disponibles`:'No disponible'}</div><button className="boton secundario" disabled={!item.disponibles}>Solicitar préstamo</button></article>)}</section></>}
